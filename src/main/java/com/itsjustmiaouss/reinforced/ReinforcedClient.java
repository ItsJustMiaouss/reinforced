package com.itsjustmiaouss.reinforced;

import net.fabricmc.api.ClientModInitializer;

public class ReinforcedClient implements ClientModInitializer {

    @Override
    public void onInitializeClient() {
//        BlockRenderLayerMap.INSTANCE.putBlock(ReinforcedBlocks.REINFORCED_GRASS, RenderLayer.getCutout());

//        ColorProviderRegistry.BLOCK.register((state, world, pos, tintIndex) ->
//                        world != null ? BiomeColors.getGrassColor(world, pos) : -1
//        , ReinforcedBlocks.REINFORCED_GRASS);
//
//        ColorProviderRegistry.ITEM.register((stack, tintIndex) -> GrassColors.getDefaultColor(), ReinforcedBlocks.REINFORCED_GRASS.asItem());
    }
}
